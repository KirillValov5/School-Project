function show (hidden)
{
    var elem=document.getElementById(hidden);
    if (elem)
        elem.style.display="block";
}
function antishow (hidden)
{
    var elem=document.getElementById(hidden);
    if (elem)
        elem.style.display="none";
}
